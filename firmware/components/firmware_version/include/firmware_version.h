#pragma once

// Zentrale Versions-/Projekt-Identitaet - von ota_manager fuer den
// Erkennungsmerkmal-Marker genutzt (siehe ota_manager.h), sonst auch fuer
// die Anzeige auf der Einstellungen-Seite. Gleiche Versionsnummer wie die
// Sensormeter-Familie zum jetzigen Zeitpunkt (Nutzervorgabe) - beide
// Projekte sind trotzdem unabhaengig, keine gemeinsame Release-Klammer,
// nur zufaellig derselbe Stand ("erste Beta").
#define FIRMWARE_PROJECT_ID "ESP-BMC"
#define DEVICE_FIRMWARE_VERSION "0.9.0-rc5"
